#include<stdio.h>
#include "main.h"

/**
* print_line - check if a character is an alphabet
* @n: the character to be checked
*
*/

void print_line(int n)
{
if (n >= 'A' && n <= 'Z')
{
putchar(n);
}


}
