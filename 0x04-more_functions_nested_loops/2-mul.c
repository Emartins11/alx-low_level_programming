#include<stdio.h>
#include "main.h"

/**
* mul - check if a character is an alphabet
* @a: the character to be checked
* @b: the character to be checked
*
* Return: 1 if lower and 0 if not
*/

int mul(int a, int b)
{
int sum;
sum = a * b;
return (sum);

}
