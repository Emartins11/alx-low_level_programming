#include <stdio.h>
#include "main.h"
#include <string.h>

/**
*_atoi - check if a character is an alphabet
*@s: the character to be checked
*Return: anything
*/

int _atoi(char *s)
{
int len;
len = strlen(s);
return (len);


}
