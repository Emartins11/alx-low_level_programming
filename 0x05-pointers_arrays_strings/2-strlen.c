#include <stdio.h>
#include "main.h"
#include <string.h>

/**
*_strlen - check if a character is an alphabet
*@s: the character to be checked
*return: anything
*/

int _strlen(char *s)
{
int len;
len = strlen(s);
return (len);


}
