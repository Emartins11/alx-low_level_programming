#include <stdio.h>
#include "main.h"

/**
*swap_int - check if a character is an alphabet
*@a: the character to be checked
*@b: the chance
*/

void swap_int(int *a, int *b)
{
int *swap;
*swap = a
*a = b;
*b = *swap;
}
