#include<stdio.h>
#include "main.h"

/**
* _abs - check if a character is an alphabet
* @n: the character to be checked
* Return: 1 if lower and 0 if not
*/

int _abs(int n)
{
if  (n >= 0)
return (n);
else
return (-1 * n);

}
