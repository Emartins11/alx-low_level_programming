#include <stdio.h>
#include "main.h"

/**
*reverse_array - check if a character is an alphabet
*@a: the character to be checked
*@n: the chance
*/

void reverse_array(int *a, int n)
{
*a = 38;
n = 50;
}
